const UNSET = typeof Symbol !== "undefined" ? Symbol("unset") : {};
const mapValues = (source, fn) => {
    const keys = Object.keys(source);
    const def = Array.isArray(source) ? [] : {};
    return keys.reduce((result, key) => {
        const value = fn(source[key], key);
        if (value !== UNSET) {
            result[key] = value;
        }
        return result;
    }, def);
};
export { mapValues, UNSET as mapValuesUNSET };
//# sourceMappingURL=mapValues.js.map