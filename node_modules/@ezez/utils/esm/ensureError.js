const ensureError = (e) => {
    if (e instanceof Error) {
        return e;
    }
    return new Error("Expected error instance, got something else: " + String(e));
};
export { ensureError, };
//# sourceMappingURL=ensureError.js.map