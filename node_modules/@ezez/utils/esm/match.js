const match = (list, fn) => {
    const matched = [];
    const unmatched = [];
    list.forEach(item => {
        if (fn(item)) {
            matched.push(item);
            return;
        }
        unmatched.push(item);
    });
    return {
        matched,
        unmatched,
    };
};
export { match, };
//# sourceMappingURL=match.js.map