const pull = (array, ...valuesToPull) => {
    const len = array.length;
    for (let i = len - 1; i >= 0; i--) {
        if (valuesToPull.includes(array[i])) {
            array.splice(i, 1);
        }
    }
    return array;
};
export { pull, };
//# sourceMappingURL=pull.js.map