"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mergeUNSET = exports.merge = void 0;
const UNSET = typeof Symbol !== "undefined" ? Symbol("UNSET") : {};
exports.mergeUNSET = UNSET;
const merge = (...args) => {
    const nonObjects = args.some(p => {
        const t = typeof p;
        return t === "string" || t === "number" || t === "symbol" || t === "boolean" || t === "function"
            || Array.isArray(p);
    });
    if (nonObjects) {
        throw new TypeError("Only objects are allowed");
    }
    const r = {};
    args.forEach((obj) => {
        Object.entries(obj || {}).forEach(([key, value]) => {
            if (value === UNSET) {
                delete r[key];
                return;
            }
            r[key] = value;
        });
    });
    return r;
};
exports.merge = merge;
//# sourceMappingURL=merge.js.map